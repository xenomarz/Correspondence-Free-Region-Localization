#pragma once#include <objective_functions/objective_function.h>#include <solvers/solver.h>class NewtonSolver : public Solver {public:	NewtonSolver(int maxIterations = MAX_ITERATIONS, double solveResidual = EPSILON, int maxLineSearchIterations = MAX_Line_Search_ITERATIONS);	virtual ~NewtonSolver();protected:	virtual void computeSearchDirection(ObjectiveFunction *function, const VectorXd &x, VectorXd& dx);private:	/*SparseMatrixd H;	std::vector<Tripletd> hessianEntries;*/};