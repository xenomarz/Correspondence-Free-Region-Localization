#pragma once#include <solvers/solver.h>class GradientDescentSolver : public Solver {public:	GradientDescentSolver(int maxIterations = MAX_ITERATIONS, double solveResidual = EPSILON, int maxLineSearchIterations = MAX_Line_Search_ITERATIONS);	virtual ~GradientDescentSolver();protected:	virtual void computeSearchDirection(ObjectiveFunction *function, const VectorXd &x, VectorXd& dx);private:	//VectorXd gradient;};